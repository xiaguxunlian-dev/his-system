# ==========================================
# HIS 测试配置 (H2 内存数据库)
# ==========================================
# 测试使用 H2 内存模式，与开发一致
db.type=h2
db.h2.name=his_test_db

# H2 内存数据库（测试专用）
db.host=localhost
db.port=0
db.name=his_test_db
db.username=sa
db.password=

# 连接池配置（测试用小值）
db.pool.size=5
db.pool.min.idle=1
db.pool.timeout=10000
db.pool.idle.timeout=300000
db.pool.max.lifetime=600000
db.pool.keepalive.time=30000

# 应用配置
app.name=医院信息管理系统（测试）
app.version=1.0.0-test
app.locale=zh_CN
hospital.name=测试医院
